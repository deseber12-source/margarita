<nav class="border-b border-slate-200 bg-white">
    <div class="mx-auto flex max-w-7xl flex-wrap items-center justify-between gap-4 px-4 py-4">
        <a href="/agent/dashboard" class="text-xl font-bold text-emerald-600">
            Margarita
        </a>

        <div class="flex flex-wrap items-center gap-3 text-sm">
            <a href="/agent/dashboard" class="font-semibold text-slate-600 hover:text-emerald-600">
                Dashboard
            </a>

            <a href="/agent/conversations" class="font-semibold text-slate-600 hover:text-emerald-600">
                Mis conversaciones
            </a>
        </div>

        <div class="flex items-center gap-3">
            <div class="text-right">
                <p class="text-sm font-semibold text-slate-800">
                    <%= user.name %>
                </p>

                <p class="text-xs text-slate-500">
                    <%= user.role %>
                </p>
            </div>

            <form action="/auth/logout" method="POST">
                <button class="rounded-xl bg-slate-900 px-4 py-2 text-sm font-semibold text-white hover:bg-slate-800">
                    Salir
                </button>
            </form>
        </div>
    </div>
</nav>
